# D6 — 不做自动轮询

- 状态：已采纳
- 日期：2026-08-18

## 决策

目录树懒加载 + 显式刷新 + 本地缓存。`max-keys` 拉满 1000。Cloudflare REST 只用于低频管理并强制缓存。

## 理由

`ListObjectsV2` 是 Class A（$4.50/百万），`GetObject` 是 Class B（$0.36/百万）。浏览比下载贵 12.5 倍。REST 全账号 1200 次/5 分钟，更不能用来列对象。
