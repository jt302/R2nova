# homebrew-r2nova

Tap for the unsigned r2nova Cask (macOS 13+).

```bash
brew tap jt302/r2nova
brew install --cask r2nova
brew upgrade --cask r2nova
```

This directory is the template for a separate `homebrew-r2nova` GitHub repo. Update `sha256` when cutting a signed or hashed release.
